
import React from 'react';

// Placeholder component to prevent build errors for the empty file.
// The QuickOrder functionality was removed per request, but the file remains to avoid import breaks if any exist.
export const QuickOrder: React.FC = () => {
  return null;
};

export default QuickOrder;
